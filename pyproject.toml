[build-system]
requires = ["setuptools>=68", "wheel", "pybind11>=2.11"]
build-backend = "setuptools.build_meta"

[project]
name = "super-ik-tsinghua-1dqp"
version = "0.1.0"
description = "NERO IK/FK: Tsinghua feasible-region + weighted-limit v2 optimized (C++/pybind)"
requires-python = ">=3.9"
